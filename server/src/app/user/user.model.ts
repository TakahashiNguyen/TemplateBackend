// Enums

/**
 * User roles in the system.
 *
 * - `guest`: Represents a guest user.
 * - `admin`: Represents an admin user.
 * - `undefined`: Represents an undefined user role.
 */
export enum UserRole {
	guest = 'guest',
	admin = 'admin',
	undefined = 'undefined',
}
